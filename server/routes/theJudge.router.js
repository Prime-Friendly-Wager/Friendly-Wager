
//NFL API QUERY
week is current week -1 (always last week)
//MATCH NFL GAME IDS -duhhhhhhhhhhhh, need to add this to games
Promise.all Games.map(
    for each one ;
    UPDATE games SET home team Score, away team score, game_completed = true,
    WHERE nfl id = $1 (this game.nflid)
    UNION if (hometeamscore + hometeamspread > awayteamscore + awayteamspread) 
        UPDATE games SET winning team id = 
)
